NEXT_PUBLIC_API_BASE_URL=https://jsonplaceholder.typicode.com
# Optional: absolute origin for same-origin `/api/*` routes during SSR (defaults to http://localhost:3000)
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# Optional second backend (defaults to same as NEXT_PUBLIC_API_BASE_URL if unset)
# NEXT_PUBLIC_API_CORE_URL=https://api.example.com/core
